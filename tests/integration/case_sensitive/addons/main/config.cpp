class CfgFunctions {
    class A {
        class common {
            // note that this is upper-cased, but the directory is lower cased
            class a {file = "../Debug/fnc_a.sqf"};
        };
    };
};
